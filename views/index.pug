//- extends layout

//- block content
//-   h1= title
//-   p Welcome to webapps Lab 10 #{title}
//-   button(type="button", class="btn btn-primary" onclick="goFaster()") Press Me
  
//-   div#faster Increase the speed
//-   <br>
//-    a(href="/vehicles") Vehicles Search Results

 
//-     script.
//-       var mph = 20;
//-       var change = 1;

//-       function goFaster() {
//-         mph += change;
//-         change += 1;
//-         var div = document.getElementById("faster");
//-         div.textContent = "MPH " + mph + " with change " + change;
//-       }
      
extends layout

block content
  if (!user)
    a(href="/login") Login
    br
    a(href="/register") Register
  if (user)
    p You are currently logged in as #{user.username}
    a(href="/vehicles") Take me to vehicles
    br
    a(href="/logout") Logout
